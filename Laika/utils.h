#pragma once
#include <Windows.h>
#include <stdio.h>
#include <time.h>
#include "resolve_apis.h"

char* CAESAR(char* in);
char* CAESAR_DECRYPT(char* in);
void Sleep_(int time_to_wait);
void Message();